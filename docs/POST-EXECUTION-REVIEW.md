# مراجعة ما بعد التنفيذ — 2026-09-22

> مقيسة على تقريرين حقيقيين: هذا المستودع (بايثون) و `/workspace/upstream-src/enola` (Go).
> كل رقم هنا أُعيد إنتاجه بأمر مذكور بجانبه. الأساس: `126f9dd`.

## 1. التقدّم المقيس

| المجال | الأساس `c4ecc22` | اليوم | الهدف | الحكم |
| --- | --- | --- | --- | --- |
| layered_engineering | 1.0000 | **1.0000** | 0.80 | بلغ |
| report_clarity | 0.9200 | **1.0000** | 0.80 | بلغ |
| structure_python | 0.9592 | **0.9554** | 0.80 | بلغ |
| runtime_surface | 0.4583 | **0.8334** | 0.80 | بلغ |
| target_architecture | 0.0000 | **0.6667** | 0.80 | دون |
| load_model | 0.0000 | **0.6731** | 0.80 | دون |
| transformation_plan | 0.6667 | **0.6667** | 0.80 | دون — وتراجع من 0.9788 |
| structure_polyglot | 0.3027 | **0.5115** | 0.80 | دون |
| independent_proof | 0.0000 | **0.0000** | 0.80 | دون — يحتاج مراجعًا بشريًا |
| **الإجمالي** | **0.4785** | **0.7008** | 0.80 | 4 مجالات من 9 |

المهام: 25 منتهية · 14 مفتوحة · 4 محجوبة من 43 (بعد إضافة المعلم N11).

## 2. أخطاء في السجل صحّحناها

| المهمة | ما كان مسجّلًا | الواقع المقيس |
| --- | --- | --- |
| N9.T1 | `blocked` — «الصندوق منع مقبس localhost» | أمر القبول يعيد 0 هنا وحزمة المراجعة كاملة. صارت `done`. |
| N6.T3 | `blocked` لنفس السبب | العمل التزم والقبول ينجح، لكن المحتوى قالب مكرّر. السبب أُعيدت كتابته. |
| N7.T1 | `blocked` لنفس السبب | العمل التزم والقبول ينجح، لكنه **خفض** المؤشر الذي جاء ليرفعه. |
| N8.T1 | `blocked` | عيب في الخطة لا في الكود: `runners.py` خارج قائمة ملفات المهمة. صُحّحت القائمة وصارت `todo`. |

سبب الحجب الثلاثي كان بيئيًا: اختبار HTTP يحتاج مقبس محلي مُنع في صندوق المنفّذ، فسقطت الحزمة كلها،
فقرأ المقياس `tests_pass = 0`، فحُجبت مهام عملها مكتمل. الاختبارات الـ877 تمر هنا.

## 3. العيوب الحقيقية — مرتّبة بالأثر

### ع1 — المحرّكات الأربعة تُنتج 3023 نتيجة ولا تغيّر مؤشّرًا واحدًا

```
eaos audit .../enola --out A --skip site
eaos audit .../enola --out B --skip site --engines codegraph enola jscpd reforge
```

`import_edge` = **5691 في الحالتين**. الـ3023 نتيجة كلها من نوع واحد `engine_finding`،
ولا واحدة منها تدخل الرسم البياني.

### ع2 — CodeGraph يفهرس 66477 حافة ونستخرج منها صفرًا

سجلّ المحرّك: `Persisted 18241 nodes, 66477 edges to graph.db`.
أدواته الأربع: اثنتان ترجعان `ok` بصفر حقيقة، وواحدة `partial`، وواحدة مرفوضة برسالة
`Missing 'uri' parameter`. السبب الجذري واحد: **الأداة تُجيب عن ملف وتُرجع صفرًا عن مجلد**، ونحن نسألها عن المجلد.

| الطلب | الناتج |
| --- | --- |
| `uri=file://<مجلد المستودع>` | `edge_count: 0` |
| `uri=file://<ملف .go واحد>` | حواف import حقيقية مع عقدها |

وهذا هو تفسير `imports_resolved_outside_python = 0.0594`.

### ع3 — فشل المحرّك مبتلع بصمت

`eaos/facts/external.py` يلفّ دمج حواف CodeGraph بـ `except Exception: pass`،
وفوقه شرط ميت `if Path(...).exists() or True`. أي فشل هنا غير مرئي في أي تقرير.

### ع4 — مسارات مكتوبة يدويًا لمستودع الاختبار

`eaos/engines/codegraph.py` يجرّب `cmd/enola/main.go` و`pkg/plugin/plugin.go` و`internal/factpath/factpath.go`.
هذه معايرة على حالة الاختبار، ولم تنجح حتى على enola نفسها: `no probe landed a symbol`.

### ع5 — نموذج الحمل أعمى على الكود الحقيقي

| السؤال | مستودعنا (56 نقطة) | enola (13 نقطة) |
| --- | --- | --- |
| `data_access_calls` | 52 مُجاب | 0 |
| `result_is_bounded` | 44 مُجاب | 0 |
| `outbound_calls_protected` | 1 مُجاب | 2 مُجاب |
| `cached` · `complexity_class` · `rate_limited` · `repeats_per_iteration` · `shared_mutable_state` | **0 من 56** | **0 من 13** |

`questions_answered` = 0.2165 على بايثون و**0.0192** على Go. القبول في N5.T6 نجح لأنه قِيس على العيّنات،
والمقياس يقيس المستودعات الحقيقية. **المهام أُغلقت على العيّنة لا على الكود.**

### ع6 — N7.T1 صفّر المؤشر الذي جاء ليرفعه

المقياس يقرأ `acceptance.command` أو `acceptance.kind == 'equivalence'`.
كتبت المهمة `kind: 'command'` ووضعت الأمر في `argv`. النتيجة: 0.9365 ← **0.0**، واختبارها ينجح.
نفس فئة العيب التي أفرغت أربع وثائق سابقًا: **الاسم يتغيّر في المُنتِج ولا يتغيّر في القارئ.**

### ع7 — بوابة عدم التراجع تُهزم بالكتابة فوق الأساس

البوابة تقارن بـ `docs/capability-score.json`، وهو نفس الملف الذي تكتبه كل إعادة قياس.
N10.T1 كتب الدرجة المتراجعة فوق العالية، فصارت البوابة تقارن 0.6667 بـ 0.6667 وتمرّ.

### ع8 — القياس يجري والمحرّكات مُطفأة

كل أوامر القياس في الخطة والبوابة بلا `--engines`، فمرحلة engines تقول
`external engines were not requested for this run`. **المشروع كله عن استثمار هذه المحرّكات، والحكم يقع وهي مغلقة.**

### ع9 — قرارات معمارية بقالب واحد

تسعة قرارات على تسعة مكوّنات تحمل نفس الخيارين («لا تفعل شيئًا» مقابل «غيّر عند الحدّ الحالي»)،
ونفس جملة المقايضة، ونفس خطوات الهجرة. «لا تفعل شيئًا» ليس بديلًا هندسيًا.
المؤشر يقرأ 1.0، و`gap_matrix` يقرأ **13 صفًا كلها `unassessed`**.

## 4. ما هو سليم ولا يحتاج عملًا

- 877 اختبارًا تمر · 66 ثابتًا معلنًا وكلها مفروضة · `tools/validate.py` بلا أخطاء.
- 33 وثيقة Markdown، **لا واحدة تتجاوز ميزانيتها** على أي من التقريرين، ولا وثيقة مطلوبة غائبة.
- `report_clarity` = 1.0: كل ادعاء بدليل وناقض، والنتائج الأولى عن كود القارئ لا عن عيّناته.
- حزمة المراجعة المستقلة مبنية بلا إجابات متوقّعة ولا تعليق من المؤلف — بناء نظيف.
- شجرة العمل نظيفة والالتزامات مرتّبة مهمة بمهمة.

## 5. الخطة

المعلم **N11** في `docs/capability-plan.json` يغلق ع1 إلى ع9 بثماني مهام،
قبولُ كلٍّ منها يقيس **مستودعًا حقيقيًا** لا عيّنة.

| المهمة | تغلق | المؤشر | من ← إلى |
| --- | --- | --- | --- |
| N11.T1 | ع1 ع2 ع4 | `imports_resolved_outside_python` | 0.0594 ← 0.50 |
| N11.T2 | ع3 | لا فشل صامت | — |
| N11.T3 | ع6 | `stages_with_runnable_acceptance` | 0.0 ← 0.90 |
| N11.T4 | ع5 (بايثون) | `questions_answered` | 0.2165 ← 0.60 |
| N11.T5 | ع5 (Go) | `questions_answered` | 0.0192 ← 0.50 |
| N11.T6 | ع9 | بدائل تخص المكوّن | — |
| N11.T7 | ع7 | علامة مثبّتة لا آخر قياس | — |
| N11.T8 | ع8 | المحرّكات تعمل وقت الحكم | — |

ويبقى من الخطة الأصلية: N6.T4 و N7.T2 و N8.T1–T3 و N10.T1–T2،
و **N9.T2 لا يغلقه نموذج**: يحتاج مراجعًا بشريًا خارج المشروع.

## 6. الدرس الواحد

كل عيب في القسم 3 من فئة واحدة: **المنتِج والقارئ يتفقان على اسم ثم يختلفان عليه بصمت.**
`kind` مقابل `value['kind']`، `runtime` مقابل `domain`، `argv` مقابل `command`،
مجلد مقابل ملف، عيّنة مقابل مستودع. الاختبار ينجح لأنه يسأل المنتِج، والمقياس يفشل لأنه يسأل القارئ.
لذلك كل قبول في N11 يقيس التقرير الناتج، لا الدالة المنتِجة.
