# M1.T1 — eaos/pipeline/stages.py و eaos stages

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M0.T1

## اقرأ أولًا

- [eaos/pipeline/stages.py](../../eaos/pipeline/stages.py) — موجود

## خطوات التنفيذ

1. إعلان 13 مرحلة
2. مُدقِّق يرفض المرجع الأمامي والدورة والمالكَين والاختياري بلا سبب

## التسليم

- `eaos/pipeline/stages.py`
- `eaos/pipeline/__init__.py`
- `tests/test_pipeline_graph.py`

## حالات القبول

- تحقق كل خطوة مذكورة في البطاقة وأرفق نتيجتها؛ لا يكفي وجود الملف أو عدد الحقول.
- لا تنخفض تغطية الفحص أو يتغير نطاق الهدف بصمت؛ السلوك غير المدعوم يظهر بسبب واضح.

```bash
python -m unittest tests.test_pipeline_graph -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M1.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف eaos/pipeline
