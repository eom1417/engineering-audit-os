# M12.T1 — تثبيت corpus مستقل وبروتوكول المقارنة مبكرًا

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M3.T1

## اقرأ أولًا

- [eaos/evaluate.py](../../eaos/evaluate.py) — موجود
- [design/output-first/QUALITY-AND-EVALUATION.md](../../design/output-first/QUALITY-AND-EVALUATION.md) — موجود
- [tests/fixtures/benchmarks](../../tests/fixtures/benchmarks) — موجود

## خطوات التنفيذ

1. حدد الفرضية والعتبات قبل تشغيل holdout؛ ضم سليم ومعيب وملتبس وfixtures مقصودة وnon-Python ومشروع كبير.
2. فصل train/development/holdout وإجابات ground truth عن الملفات التي يستقبلها المحلل.
3. عرّف نفس النموذج والأدوات والميزانية للوكيل العادي ولأداتنا، مع تسجيل كل تدخل بشري.
4. رتّب تحكيم الادعاءات والبطاقات بواسطة شخص لم يكتب إجابات الحالة؛ إن لم يتوفر فالحالة blocked لا independent.

## التسليم

- `evaluations/protocol.json`
- `evaluations/corpus-manifest.json`
- `tests/test_eval_protocol.py`

## حالات القبول

- أي ادعاء غير محكم لا يحتسب صحيحًا.
- ملف ground truth لا يدخل السياق أو source inventory.
- البروتوكول يثبت القيود والمقاييس والميزانيات قبل القياس.

```bash
python -m unittest tests.test_eval_protocol -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M12.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تدّعِ اكتمال معنى أو جودة لم تُختبر؛ لا توسّع الدعم لكل اللغات ضمن هذه المهمة.

احتفظ بالصيغة القديمة واقرأها دون تغيير؛ اعزل التغيير في مهمة قابلة للتراجع ولا تعِد تصنيف سجل تاريخي كنجاح.
