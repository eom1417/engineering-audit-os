# M3.T1 — eaos audit و RUN.md

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M2.T1

## اقرأ أولًا

- [eaos/pipeline/report.py](../../eaos/pipeline/report.py) — موجود

## خطوات التنفيذ

1. أمر audit
2. review-project يصير غلافًا
3. RUN.md تملكها validate

## التسليم

- `eaos/cli.py`
- `eaos/product_review.py`
- `eaos/pipeline/report.py`

## حالات القبول

- تحقق كل خطوة مذكورة في البطاقة وأرفق نتيجتها؛ لا يكفي وجود الملف أو عدد الحقول.
- لا تنخفض تغطية الفحص أو يتغير نطاق الهدف بصمت؛ السلوك غير المدعوم يظهر بسبب واضح.

```bash
eaos audit . --out /tmp/m3 --engines && python3 -c "import os;assert all(os.path.exists('/tmp/m3/'+f) for f in ('SUSTAINABILITY.md','ENGINES.md','probes.json','RUN.md','transform-plan.md','run-manifest.json'))"
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M3.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

git revert
