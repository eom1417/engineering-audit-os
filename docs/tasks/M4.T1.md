# M4.T1 — سجل قدرات المسار القديم قبل حذف أي شيء

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M3.T1

## اقرأ أولًا

- [eaos/cli.py](../../eaos/cli.py) — موجود
- [eaos/workflow.py](../../eaos/workflow.py) — موجود
- [eaos/runtime/pipeline.py](../../eaos/runtime/pipeline.py) — موجود
- [tests/test_workflow.py](../../tests/test_workflow.py) — موجود
- [tests/test_runtime.py](../../tests/test_runtime.py) — موجود

## خطوات التنفيذ

1. لكل أمر من الـ19: سجّل {command, handler, modules, tests, what_it_does, modern_equivalent, verdict}
2. verdict واحد من: replaced (للحديث بديل كامل) · migrate (قدرة فريدة تصير مرحلة) · keep (تبقى بأمر مستقل) · delete (بلا قيمة)
3. القدرات المرشّحة لـmigrate المعروفة سلفًا: packet (حزمة سياق محدودة بميزانية أحرف على مصدر غير موثوق) و implement/improve (حلقات تعديل يقودها نموذج)
4. لا تقرر من الاسم؛ اقرأ الدالة وشغّل الأمر على مشروع اختبار
5. عدد 19 خط أساس تاريخي وليس ثابتًا: استخرج قائمة CLI الحالية وغطّ كل قدرة قديمة مع دليل اختبار تكافؤ أو فجوة واضحة.

## التسليم

- `docs/legacy-inventory.json`
- `tests/test_legacy_inventory.py`

## حالات القبول

- السجل يغطي كل أمر قديم مستخرج من CLI الحالي، لا قائمة 19 مثبتة يدويًا.
- لكل قرار migrate/replaced دليل تكافؤ محدد أو فجوة واختبار يجب نقله؛ لا يكفي modern_equivalent غير فارغ.

```bash
python -m unittest tests.test_legacy_inventory -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M4.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف الملف؛ لا تغيير في الكود
