# M4.T2 — نقل المساعدَين المشتركَين إلى core

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M4.T1

## اقرأ أولًا

- [eaos/claims.py](../../eaos/claims.py) — موجود
- [eaos/dossier.py](../../eaos/dossier.py) — موجود

## خطوات التنفيذ

1. انقل schema_errors إلى وحدة core مستقلة (مثلاً eaos/schema.py) وحدّث claims.py
2. انقل classify إلى نفس الوحدة أو إلى eaos/facts/source.py إن كانت تصنيف ملفات، وحدّث dossier.py
3. تأكد أن لا وحدة حديثة تستورد من المسار القديم: grep يجب أن يعود فارغًا

## التسليم

- `eaos/audit_records.py`
- `eaos/discovery.py`
- `eaos/claims.py`
- `eaos/dossier.py`
- `eaos.policy.json`
- `tests/test_legacy_boundaries.py`

## حالات القبول

- اختبار imports يمنع عودة اعتماد حديث على وحدة قديمة حتى إن تغير تنسيق import.
- اختبارات schema_errors وclassify القديمة والجديدة تعطي النتائج نفسها على السليم والمعيب.

```bash
python -m unittest tests.test_legacy_boundaries tests.test_claims -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M4.T2.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

git revert
