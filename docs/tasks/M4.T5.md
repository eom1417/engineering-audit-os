# M4.T5 — واجهة أوامر مجمّعة ومُتحقَّق منها

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M4.T4

## اقرأ أولًا

- [eaos/cli.py](../../eaos/cli.py) — موجود

## خطوات التنفيذ

1. جمّع الأوامر في المساعدة: تشغيل · استعلام · حوكمة · أدوات
2. اختبار: لا أمران ينتجان نفس المصنوعة (من عقد المصنوعات في M5)
3. اختبار: كل أمر معروض له وصف help غير فارغ

## التسليم

- `eaos/cli.py`
- `tests/test_cli.py`

## حالات القبول

- --help يوضح المدخل الأساسي ومجموعات الأوامر ومسار الترحيل.
- اختبار CLI ينفذ المدخل الجديد ومدخل التوافق ويقارن الحالة والنتائج لا النص فقط.

```bash
python -m unittest tests.test_cli -q && eaos --help | grep -q 'تشغيل\|Run'
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M4.T5.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

git revert
