# M5.T1 — إعلان عقد المصنوعات

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M3.T1

## اقرأ أولًا

- [eaos/compose/rules.py](../../eaos/compose/rules.py) — موجود
- [eaos/pipeline/stages.py](../../eaos/pipeline/stages.py) — موجود

## خطوات التنفيذ

1. ARTIFACTS: لكل ملف {name, owner_stage, purpose, reading_order, budget_lines, required, absent_when}
2. BUDGETS في compose/rules.py تُشتق من هذا العقد لا تُكرَّر — مصدر واحد
3. المراحل في pipeline/stages.py تُشتق produces منه أو تُطابَق به باختبار

## التسليم

- `eaos/compose/artifacts.py`

## حالات القبول

- العقد يرفض معرف مصنوعة مكررًا أو مالكًا مفقودًا أو reference غير معروف.
- مصنوعة optional تحمل شرط غياب واضحًا، وملف legacy لا يخالف نسخة العقد بصمت.

```bash
python -m unittest tests.test_artifact_contract -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M5.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف الوحدة وإعادة BUDGETS كما كانت
