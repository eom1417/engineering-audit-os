# M5.T2 — README مولَّد من العقد

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M5.T1

## اقرأ أولًا

- [eaos/dossier.py](../../eaos/dossier.py) — موجود

## خطوات التنفيذ

1. index_document يقرأ العقد ويرتّب بـreading_order
2. كل صف يذكر الغرض والمالك
3. الغائب يظهر بسبب غيابه من run-manifest

## التسليم

- `eaos/dossier.py`
- `eaos/compose/artifacts.py`
- `tests/test_artifact_navigation.py`

## حالات القبول

- كل رابط في الفهرس يشير إلى مصنوعة فعلية أو حالة عدم توليد مع سبب.
- ترتيب القراءة يبدأ بالقرار والفهم ثم الأدلة؛ إضافة مصنوعة للعقد تحدث الفهرس دون قائمة ثانية.

```bash
python -m unittest tests.test_artifact_navigation -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M5.T2.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

git revert
