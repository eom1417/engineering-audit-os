# M5.T3 — قاعدة R13 في عقد المخرجات

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M5.T2

## اقرأ أولًا

- [eaos/compose/rules.py](../../eaos/compose/rules.py) — موجود

## خطوات التنفيذ

1. R13: كل ملف في out/ معلن في العقد
2. R13: كل معلَن required منتَج أو مُعلَّل في run-manifest
3. اختبار طفرة: أضف ملفًا غير معلن → يجب أن تفشل القاعدة

## التسليم

- `eaos/compose/rules.py`
- `tests/test_compose_rules.py`

## حالات القبول

- حذف مصنوعة required أو إدخال رابط معلق يفشل R13.
- غياب optional مع سبب مشروع يمر؛ وجود اسم الملف وحده مع محتوى غير متسق لا يثبت جاهزية التقرير.

```bash
python -m unittest tests.test_artifact_contract -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M5.T3.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

إسقاط R13 من قائمة القواعد
