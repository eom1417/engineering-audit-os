# M6.T1 — استخراج الثوابت المعلنة

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M3.T1

## اقرأ أولًا

- [eaos/compose/__init__.py](../../eaos/compose/__init__.py) — موجود
- [eaos/facts/__init__.py](../../eaos/facts/__init__.py) — موجود
- [eaos/claims.py](../../eaos/claims.py) — موجود

## خطوات التنفيذ

1. tools/invariants.py --list يمشي على eaos/**.py ويستخرج كل docstring يطابق نمط الادعاء
2. يكتب docs/invariants.json: {id, file, symbol, statement, enforced_by: null}
3. الـid مشتق من المحتوى ليبقى ثابتًا عبر التشغيلات

## التسليم

- `tools/invariants.py`
- `docs/invariants.json`
- `tests/test_invariant_inventory.py`

## حالات القبول

- عينة معروفة من assertions تكتشف مع مسار وسطر؛ وصف محايد لا يصنف invariant إلزاميًا.
- النتيجة تعرض قيود الاستخراج؛ بلوغ عدد 40 ليس معيار اكتشاف صحيح.

```bash
python -m unittest tests.test_invariant_inventory -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M6.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف الأداة والملف
