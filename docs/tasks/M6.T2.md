# M6.T2 — ربط كل ثابت باختبار يفرضه

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M6.T1

## اقرأ أولًا

- [tests/](../../tests/) — موجود

## خطوات التنفيذ

1. لكل ثابت: ابحث عن اختبار يفرضه فعلًا (لا اختبار يذكره)
2. enforced_by = 'tests/test_x.py::Class::test_name' أو null
3. الثابت الذي لا تجد له اختبارًا: علّمه needs_test=true

## التسليم

- `docs/invariants.json`
- `tests/test_invariant_enforcement.py`

## حالات القبول

- وجود اسم اختبار لا يثبت enforcement: شغّل حالة خرق للثابت وبيّن أن الاختبار يفشل بسبب الخرق.
- احذف الادعاء غير الصحيح أو صنّفه limitation؛ لا تكتب اختبارًا يعيد مقارنة النص بنفسه.

```bash
python -m unittest tests.test_invariant_enforcement -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M6.T2.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

لا تغيير في الكود
