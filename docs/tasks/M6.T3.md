# M6.T3 — سدّ الفجوة: اختبار أو حذف الجملة

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M6.T2, M6.T1

## اقرأ أولًا

- [docs/invariants.json](../../docs/invariants.json) — ينتجه M6.T1

## خطوات التنفيذ

1. لكل needs_test: اكتب اختبارًا يفشل لو خُرق الثابت (اختبر الخرق، لا الحالة السليمة)
2. إن كان الثابت غير قابل للاختبار أو غير صحيح: احذف الجملة من الـdocstring وسجّل ذلك
3. دفعة لكل وحدة، التزام مستقل

## التسليم

- `tests/`
- `eaos/`

## حالات القبول

- لكل needs_test اختبار خرق يفشل قبل إصلاحه أو تصحيح موثق للادعاء غير الصحيح.
- لا تتغير حدود الضمان لتوافق الاختبار مع بقاء ادعاء أوسع في الوثائق.

```bash
python tools/invariants.py --check
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M6.T3.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

كل دفعة التزام مستقل
