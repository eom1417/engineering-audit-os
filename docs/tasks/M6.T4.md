# M6.T4 — إدخال الفحص في بوابة العبور

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M6.T3

## اقرأ أولًا

- [tools/validate.py](../../tools/validate.py) — موجود

## خطوات التنفيذ

1. tools/validate.py يستدعي فحص الثوابت ويُدرج نتيجته في errors
2. أي docstring جديد يدّعي ثابتًا بلا اختبار يُفشل البوابة

## التسليم

- `tools/validate.py`
- `docs/transformation.json`

## حالات القبول

- إضافة assertion إلزامي بلا دليل enforcement تفشل البوابة مع موقعه.
- القيود الوصفية ونتائج not_adjudicated لا تُعرض كضمانات مختبرة.

```bash
python tools/validate.py && python3 -c "import json,subprocess;o=json.loads(subprocess.run(['python','tools/validate.py'],capture_output=True,text=True).stdout);assert 'invariants' in o"
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M6.T4.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

إرجاع الفحص تحذيرًا
