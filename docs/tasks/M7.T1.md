# M7.T1 — eaos baseline pin/show/clear

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M5.T3

## اقرأ أولًا

- [eaos/delta.py](../../eaos/delta.py) — موجود
- [eaos/pipeline/run.py](../../eaos/pipeline/run.py) — موجود

## خطوات التنفيذ

1. pin: خزّن dossier.json و run-manifest.json و بصمة الهدف تحت <out>/baseline/
2. show: اطبع متى ثُبِّت وعلى أي التزام وكم ادعاءً يحمل
3. clear: احذف مع تأكيد
4. لا تكتب داخل المشروع الهدف — الهدف للقراءة فقط

## التسليم

- `eaos/baseline.py`
- `eaos/cli.py`

## حالات القبول

- pin يحفظ نسخة وبصمة وإصدارات يمكن إعادة تحميلها ولا يكتب داخل الهدف.
- لا يمسح clear سجلًا آخر؛ مدخل غير موجود يعطي خطأ واضحًا؛ إعادة pin لا تستبدل التاريخ بصمت.

```bash
eaos audit . --out /tmp/m7 && eaos baseline pin --out /tmp/m7 && eaos baseline show --out /tmp/m7 | grep -q claims
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M7.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف الوحدة والأمر
