# M7.T3 — سيناريو البوابة كاختبار

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: todo · يعتمد على: M7.T1

## اقرأ أولًا

- [tests/fixtures/benchmarks](../../tests/fixtures/benchmarks) — موجود

## خطوات التنفيذ

1. أنشئ fixture وبروتوكول بوابة يختبر دينًا سابقًا لا يمنع التبني، ومخالفة جديدة مثبتة تفشل، وإزالة جديدة تتحقق سلوكيًا.
2. أضف حالات تغير extractor والنطاق، ونتيجة غير مؤكدة، واستثناء موثق؛ لا تسوّها بإصلاح.
3. اكتب الاختبارات أولًا؛ اجعل نتيجة هذه المهمة قبول وجود عقد السيناريو واختبار بياناته، أما نجاح سلوك الأمر فشرط M7.T2.

## التسليم

- `tests/gate/no_new_debt.sh`
- `tests/fixtures/debt/`
- `tests/test_debt_fixture.py`

## حالات القبول

- تحقق كل خطوة مذكورة في البطاقة وأرفق نتيجتها؛ لا يكفي وجود الملف أو عدد الحقول.
- لا تنخفض تغطية الفحص أو يتغير نطاق الهدف بصمت؛ السلوك غير المدعوم يظهر بسبب واضح.

```bash
python -m unittest tests.test_debt_fixture -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M7.T3.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف السيناريو
