# M8.T1 — عيّنات مخرَج مثبّتة لكل محرك

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M3.T1

## اقرأ أولًا

- [eaos/engines/enola.py](../../eaos/engines/enola.py) — موجود
- [eaos/engines/reforge.py](../../eaos/engines/reforge.py) — موجود
- [eaos/engines/jscpd.py](../../eaos/engines/jscpd.py) — موجود
- [eaos/engines/codegraph.py](../../eaos/engines/codegraph.py) — موجود

## خطوات التنفيذ

1. خزّن مخرَجًا حقيقيًا مصغّرًا لكل محرك من الإصدار المثبَّت
2. اختبار: المحوّل يطبّع العيّنة إلى النتائج المتوقعة بلا شبكة وبلا ثنائي مثبّت

## التسليم

- `tests/contracts/enola.json`
- `tests/contracts/reforge.json`
- `tests/contracts/jscpd.json`
- `tests/contracts/codegraph.json`

## حالات القبول

- كل fixture يذكر نسخة المحرك ومصدره وبصمته ويعمل الاختبار دون binary أو شبكة.
- إزالة حقل إلزامي أو تغيير schema إلى غير مدعوم ينتج unsupported واضحًا لا نتائج فارغة ناجحة.

```bash
python -m unittest tests.test_engine_contracts -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M8.T1.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف العيّنات
