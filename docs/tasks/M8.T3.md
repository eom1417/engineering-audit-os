# M8.T3 — قياس دقة المحركات على حالات معنونة

> مولَّد من docs/transformation.json؛ الحالة هناك هي المرجع.

الحالة: done · يعتمد على: M8.T2, M12.T1

## اقرأ أولًا

- [eaos/evaluate.py](../../eaos/evaluate.py) — موجود
- [eaos/correlate.py](../../eaos/correlate.py) — موجود

## خطوات التنفيذ

1. ابنِ حالات فيها مشكلة معروفة: دورة، تكرار حرفي، دالة معقّدة، كود ميت، خرق حد
2. شغّل كل محرك وسجّل precision/recall لكل نوع
3. النتيجة تُغذّي أوزان الربط في correlate.py بدل أوزان مُخمَّنة
4. all claims تُحكّم correct/incorrect/uncertain/not_adjudicated؛ لا تقيس precision من substring. لا تضبط الأوزان على holdout ولا تُعد المحرك الغائب نتيجة صفر أخطاء.

## التسليم

- `tests/fixtures/benchmarks/engines/`
- `eaos/evaluate.py`
- `tests/test_engine_evaluation.py`

## حالات القبول

- كل نتيجة تُحكّم أو تظل غير محكمة ولا تدخل numerator الصحيح.
- حالة محرك غير موجود وتعارض تفسير ونتيجة صحيحة كهيكل لكنها لا تبرر إصلاحًا تظهر منفصلة.

```bash
python -m unittest tests.test_engine_evaluation -q
```

**تنبيه التنفيذ:** الاختبارات أو الأدوات الجديدة المذكورة تُكتب داخل المهمة؛ الأمر ليس دليلًا على أنها موجودة أو ناجحة الآن.

**قبول مستقل مطلوب:** لا؛ تبقى مراجعة صحة الدليل لازمة.

**سجل الدليل:** `docs/task-results/M8.T3.json` وفق evidence_contract في المصدر.

## الحدود والتراجع

- لا تضف واجهة سحابية أو تعدل كود المنابع أو تعيد كتابة محلل خارجي.

حذف الحالات
