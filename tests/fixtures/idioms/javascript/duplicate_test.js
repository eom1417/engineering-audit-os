function flow_a(req) {
    validate(req);
    save(req);
    notify(req);
    return req;
}
function flow_b(req) {
    validate(req);
    save(req);
    notify(req);
    return req;
}
function flow_c(req) {
    validate(req);
    save(req);
    notify(req);
    return req;
}
