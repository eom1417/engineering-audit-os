name: ci
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: install
        run: pip install -r requirements.txt
      - name: lint
        run: ruff check
      - name: test
        run: pytest
